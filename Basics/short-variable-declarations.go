package main

import "fmt"

/*
	1. ":=" is the same thing as using var
	2. The above cannot be used outside of a function -> all
		statements outside of a function must have a keyword
*/

func main() {
	var i,j int = 1,2
	k := 3
	c,python,java := true,false "no!"
}